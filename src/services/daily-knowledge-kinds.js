/**
 * Controlled daily_knowledge.kind values.
 * Never duplicate these string literals elsewhere — import this constant.
 */
const DAILY_KNOWLEDGE_KIND = Object.freeze({
  COMPETITOR: 'competitor',
  OWN_ADS: 'own_ads',
});

module.exports = { DAILY_KNOWLEDGE_KIND };
